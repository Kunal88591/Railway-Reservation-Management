#!/bin/bash
# Quick Start Launcher for Railway Reservation System
# Double-click or run this file to start the application

# Get the directory where this script is located
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Change to that directory
cd "$DIR"

# Run the application
./run.sh
